import { BoardContainer } from "./board.styles";
import { Card } from "../card/card.component";

export const Board = () => {
  return (
    <BoardContainer>
      <Card />
    </BoardContainer>
  );
};

export default Board;
