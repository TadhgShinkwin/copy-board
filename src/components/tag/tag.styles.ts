import styled from "styled-components";

export const TagOuter = styled.div`
    border: 1px solid #dfe5eb;
    border-radius: 8px;
    display: flex;
    flex-direction: row;
    font-size: 0.8em;
    height: fit-content;
    padding: 0 3px;
    color:rgb(133, 145, 156);
    cursor: pointer;
`;

export const TagIcon = styled.p`
    padding: 3px;
`;

export const TagTitle = styled.p`
    padding: 3px;
`;
